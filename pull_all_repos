# for repo in `cat example_repos/repo_list`; do git clone $repo && pushd $repo && git pull && popd ; done
cd /root/radar_repos
for repo in `ls /root/radar_repos/`; do pushd $repo && git pull && popd ; done
